## Live2D 当前问题与原因临时笔记

### 1. 侧窗 / 悬浮窗不张嘴
- 浏览器页面里的 Live2D 能正常嘴型同步，说明后端推送的音频消息和 `playAudioBase64` / `playAudioUrl` 流程是工作的。
- 侧窗 WebView 打开的也是 `static/live2d/index.html`，但很可能没有收到或没有消费音频消息，因此没有调用上述播放函数。
- 另一种可能是 WebView 内的页面没有被有效的用户手势解锁 `AudioContext`，导致音频解码虽成功但没有真正驱动分析节点。
- 根因：侧窗这一份页面实例没有真正启动唇形循环，而非嘴型算法失效。

### 2. 嘴型拖尾（音频结束后嘴还动 2–3 秒）
- 唇形分析使用 RMS + 多频段能量估算嘴的开合和形状，并加入了较高的平滑系数以减少抖动。
- 为避免“首句开口太小看不到嘴动”，在开头注入了轻微运动与初始开口，并对静音区做了短时间的“假运动”增强。
- 当前静音阈值较低，加上 TTS 尾部残留噪声和混响，RMS 很难迅速归零，导致平滑后的嘴型在主观听到“已经安静”之后仍然继续缓慢运动 2–3 秒。
- 根因：静音判定与平滑/假运动策略偏保守，未对“持续静音”设置更快收尾逻辑，同时对背景噪声容忍度过高。

### 3. MotionAgent / LLM 连接错误与 ResourceWarning
- 日志中的 `LLM Error: Connection error`、`MotionAgent Error: Connection error` 表示动作智能体在调用远程 LLM 或动作服务时网络失败。
- 为了不让角色完全僵住，在错误时会采用一组中性的 fallback 参数继续驱动头部和身体的轻微动作。
- Python 中的 `ResourceWarning: unclosed socket/transport` 源于这些网络错误后连接未被完全清理，属于资源管理层面的警告。
- 根因：远程服务不稳定或配置问题导致连接失败，外加部分网络客户端未在异常路径中正确关闭。

### 4. 待机动作与“抠出”现象
- 模型自带的 Idle 动画与自定义的“Neuro 行为系统”会在非说话状态下随机调整头部、眼睛和身体姿态。
- 随机目标值跨度过大或切换过快时，插值不足以掩盖突变，会出现明显的姿态跳变，也就是“抠出”的感觉。
- 说话状态下如果 idle 层依然强势叠加在语义动作或嘴型之上，可能与物理/动作系统产生冲突，形成短时间的抽搐。
- 根因：Idle/Neuro 动作缺乏对速度、加速度的约束，也缺乏与“正在说话”状态的协调。

### 5. 多端嘴型不同步的问题
- 当前每个 Live2D 页面实例本地解析自己的音频流，依赖各自的调度与分析结果。
- 浏览器页面与侧窗/悬浮窗如果没有严格共享同一份音频驱动信息，会出现嘴型时间轴不一致，甚至某一端完全不动嘴的情况。
- 根因：嘴型驱动源分散在多个页面实例中，没有统一的“时间 → 嘴型曲线”作为权威来源。

